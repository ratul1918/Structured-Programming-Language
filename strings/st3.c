#include<stdio.h>
#include<string.h>

int main() {
    char s1[] = "Rafiur Rahman Ratul"; // Declare and initialize a string
    int len = strlen(s1);              // Get the length of the string (excluding null terminator)
    printf("%d", len);                 // Print the length
    return 0;
}
